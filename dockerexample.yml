services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.prod
    container_name: ragchat-backend-prod
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env.prod
    restart: unless-stopped

